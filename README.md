This is a bootable 32 bit kernel (and eventually small os) I have been working on with the end goal of getting the dotnet runtime working on it. I'm probably not very good at this but it's fun.
If for whatever reason you want to boot this, just use the makefile and mount disk.img as a floppy drive on a virtual machine. I haven't tested it on real hardware yet.
